import React from "react";

const SingleItem = (post) => {
//   console.log(post);
//   console.log(post.title);
//   console.log(post.subtitle);
//   console.log(post.selectedColor);
  return (
    <div>
      {/* <div>{post.title}</div>
      <div>{post.subtitle}</div>
      <div>{post.selectedColor}</div> */}
    </div>
  );
};

export default SingleItem;
